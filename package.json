{
  "name": "single-video-player-attract-video",
  "version": "1.0.0",
  "description": "Based from Electron quick start tutorial last followed 4-6-2022",
  "main": "./src/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron --inspect=5858 .",
    "package-win": "electron-packager . single-video-player-attract-video --overwrite --asar=false --platform=win32 --arch=ia32 --icon=common/assets/icons/ncma-icon.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Single Video Player\""
  },
  "author": "Kevin Kane",
  "license": "MIT",
  "dependencies": {
    "electron": "^18.0.2"
  },
  "devDependencies": {
    "tailwindcss": "^3.0.24"
  }
}
